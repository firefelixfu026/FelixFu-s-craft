# 版本日志

这里记录 FelixFu's Craft 从 MVP 到当前版本的主要演进。小修小补更新小版本；较大的体验迭代、全新功能或结构调整更新中/大版本。

## v6.9.3 - Live Preview 编辑手感重构

- 实时预览改用 CodeMirror Markdown 语法树，只装饰可见内容，避免链接、嵌套格式和代码块被正则误判。
- 当前编辑行恢复 Markdown 标记但保留最终文字样式，切换行时不再发生明显的字号和布局跳变。
- 块公式改由编辑器状态管理，支持单行与多行 `$$` 公式；点击渲染结果可直接回到公式源码。
- 列表、任务框和围栏代码块增加接近 Obsidian Live Preview 的就地样式，代码中的美元符号不会触发公式渲染。

## v6.9.2 - Obsidian 风格实时编辑

- 左侧始终保持可编辑、右侧始终保持只读；自动渲染开启后，左侧 Markdown 片段在光标离开时就地渲染，回到片段时恢复源码。
- 支持公式、标题、加粗、删除线、高亮、行内代码和链接的实时装饰渲染，公式由 KaTeX 呈现。
- 修复 `Ctrl/⌘ + Shift + 7/8` 快捷键的物理按键判断。
- 编辑器改为同步事务更新，代码块内按 Enter 后光标不再因延迟恢复而闪烁。

## v6.9.1 - 写作台实时渲染与围栏修复

- 代码模式只在成对 Markdown 围栏之间启用，未闭合围栏不再错误触发自动缩进。
- 输入第三个反引号时自动生成结束围栏，并把光标放到中间代码行。
- 自动渲染开启后，左侧显示实时正文，右侧切换为 Markdown 源码编辑；关闭时恢复左侧源码、右侧正文预览。
- 新增 `Ctrl/⌘ + Z` 撤回、`Ctrl/⌘ + Shift + Z` / `Ctrl/⌘ + Y` 重做，并让 `Ctrl/⌘ + S` 直接保存文章。

## v6.9.0 - 写作台代码模式

- `Tab` 统一插入 4 个空格，`Shift+Tab` 支持按行减少最多 4 个前导空格。
- Markdown 围栏代码块内新增智能回车缩进、括号与引号自动闭合、重复右符号跳过和空符号对删除。
- 写作台新增自动渲染开关；关闭时预览区切换为等宽字体 Markdown 源码视图，并记住上次选择。

## v6.8.6 - 部署脚本权限修复

- 生产拉取忽略 Windows 与 Linux 之间的可执行权限位差异，避免服务器工作区被误判为含有本地修改。
- GitHub Actions 改为通过 `bash` 执行部署脚本，不再在服务器仓库中运行会留下权限变更的 `chmod`。

## v6.8.5 - 生产部署稳定性修复

- 修复生产部署脚本的 UTF-8 BOM 与 Windows 换行问题，避免 Linux 无法识别脚本解释器。
- GitHub Actions 拉取生产代码时强制使用 HTTP/1.1，并在临时网络中断时自动重试三次。
- 自动部署只拉取一次代码，避免部署脚本重复访问 GitHub 导致无谓失败。

## v6.8.4 - 新服务器迁移记录

- FelixFu's Craft 已迁移到杭州新 ECS，站点容器、PostgreSQL 数据和上传资源完成恢复。
- 生产部署流程继续使用 GitHub Actions 拉取最新代码并在 `/opt/felixfu-blog` 重建服务。
- 备案完成前，主域名访问会受到阿里云备案拦截影响，可先通过服务器公网 IP 验证在线状态。

## v6.8.3 - 大陆服务器构建源优化

- 前端 Docker 构建默认使用国内 npm 镜像源，并增加依赖下载重试和超时设置。
- 后端 Docker 构建默认使用国内 PyPI 镜像源，减少杭州 ECS 首次部署时依赖下载超时。
- 保持 Docker Compose 使用方式不变，服务器拉取最新代码后可直接重新构建。

## v6.8.2 - 计划数据防覆盖保护

- 计划页加载和模板补全不再自动写回数据库，只有管理员实际编辑后才触发保存。
- 应用使用时间和完成度会尝试从浏览器本地缓存与恢复缓存中找回 8.4-8.15 的历史填写内容。
- 后端保存计划前会保留上一版计划快照，后续误覆盖时更容易回溯。

## v6.8.1 - 在家阶段计划恢复与总结

- 恢复 8.4-8.15 暑期在家计划任务，不再被 8.16-9.11 在校阶段覆盖。
- 计划、应用和完成度按日期合并模板与数据库记录，历史阶段和新阶段并行显示。
- 计划页新增“总结”入口，自动汇总在家阶段的完成度、饮食、体重、记账、睡眠和应用记录。
- 修正上一版阶段说明，避免误解为旧计划被迁移隐藏。

## v6.8.0 - 暑期在校计划阶段切换

- 暑期在校阶段新增 8.16-9.11 空任务清单，方便自己从零填写当天目标。
- 饮食、记账、身体和睡眠新增记录会根据当前日期和现有最后日期自动顺延，睡眠新增记录自动生成跨日范围。
- 记账模块新增收入/支出来源饼图，以及每日收支折线图和柱状图。

## v6.7.2 - 计划记录修复和后台入口瘦身

- 修复完成度记录被旧任务兜底数据覆盖，管理员填写实际完成内容后会按当前每日任务保存。
- 管理工具台移除健康、视觉巡检、语料和学习助手入口，让后台更清爽。
- 手机应用默认目标统一为 60 分钟，并补上红果短剧和原神。
- 记账记录按日期自动排序。
- 体重和睡眠图表忽略空值并自动放大有效波动，睡眠时长根据入睡和起床时间自动计算。

## v6.7.1 - 完成度编辑提示优化

- 完成度表格把计划任务和完成标准改为文本展示，避免误以为只读输入框坏了。
- 实际完成内容、状态和备注继续作为可编辑记录项。
- 补充完成度只跟随每日任务清单的说明。

## v6.7.0 - 暑期计划改为每日任务清单

- 计划页从时间段安排改为每天必做任务清单，吃饭、轻复盘等低价值格子不再挤进主计划。
- 完成度统计改为跟随每日任务，记录任务类型、完成标准、实际完成内容和状态。
- 复制 Markdown 输出同步改成任务清单格式，方便之后继续微调。

## v6.6.3 - 后台入口网格收敛

- 后台入口改回统一网格，去掉三块大分组造成的割裂感。
- 保留备份、语料、学习助手等新增入口，但用更紧凑的同尺寸卡片呈现。

## v6.6.2 - 后台分区导航和写作缩进

- 后台导航按日常、资源、维护分区，备份、语料和学习助手不再藏在总览里。
- 写作台支持 `Tab` 与 `Shift+Tab` 缩进，段落预览更尊重空格和换行。
- 文章目录跳转增加顶部余量，阅读页暗色目录更清楚。

## v6.6.1 - 后台健康面板初始化修复

- 修复后台健康面板和导航统计读取错误日志状态过早导致的 `/admin` 白屏。
- 保留后台局部错误兜底，后续模块异常会更容易定位。

## v6.6.0 - 后台体验和工具箱批量能力

- 后台新增站点健康面板，集中查看文章、评论、音乐、错误日志和部署状态。
- 后台内容区加入局部错误兜底，单个模块异常不再拖垮整页。
- 工具箱支持 JSON 批量导入导出，方便备份和迁移常用网址。
- 写作台补充快捷键提示和常用 Markdown 快捷操作。
- Live2D 模型延后到浏览器空闲时挂载，减轻首屏压力。

## v6.5.7 - 后台工具箱崩溃修复

- 后台工具箱接入正确的图片上传函数，修复 `uploadAdminImage is not defined` 导致的整页崩溃。
- 管理页友链封面上传与前台工具箱复用同一套上传逻辑。

## v6.5.6 - Live2D 切换贴图闪烁修复

- 切换模型和正常加载阶段不再显示兜底贴图。
- 只有 Live2D 真正加载失败时才展示静态贴图，避免凯尔希切换时短暂露出素材图。

## v6.5.5 - Live2D 切换动效收敛

- 移除 Live2D 切换模型时的加载中文字，减少视觉打断。
- 新模型挂载时增加轻量淡入，让黍泡泡和凯尔希之间切换更顺滑。

## v6.5.4 - Live2D 热切换稳定补丁

- 右键切换模型和重试加载会先销毁旧 PIXI 舞台，再重建 canvas。
- 模型切换等待浏览器完成两帧渲染后再挂载新模型，避免旧 WebGL 实例残留导致崩溃。
- 刷新页面、关闭再显示和页面内切换使用同一套干净挂载流程。

## v6.5.3 - Live2D 模型切换补丁

- 凯尔希模型入口移除损坏的物理和显示信息引用，避免 JSON 解析失败导致加载失败。
- 右键菜单恢复 Live2D 重试加载入口。
- 关闭提示和账号页显示按钮会跟随当前模型名称变化。

## v6.5.2 - Live2D 模型切换

- 新增凯尔希 Live2D 模型资源，并整理为稳定英文路径。
- 黍泡泡右键菜单新增模型切换入口，可在黍泡泡和凯尔希之间切换。
- 模型名称、来源和静态兜底图会跟随当前模型变化。

## v6.5.1 - 播放器切歌进度补丁

- 保留秒数只用于刷新页面或重新打开网站时恢复离开前正在播放的那首歌。
- 手动点歌、上一首下一首、随机切歌和列表自动下一首都会从 0 秒开始。
- 切歌瞬间不再把上一首歌的暂停进度写到下一首歌。

## v6.5.0 - 音乐、写作和备份工作流增强

- 音乐页新增“喜欢歌曲”和“播放足迹”，歌单之外也能快速回到常听内容。
- 写作台新增正文统计、常用 Markdown 插入按钮和拖拽图片上传，预览写作更接近笔记软件。
- 首页进一步瘦身，移除展示型终端和大指标块，保留入口、时钟、更新和最近文章。
- 内容库新增目录快捷筛选和收纳输入提示，批量整理笔记时更像文件管理器。
- 备份中心支持导入 JSON 预检，并可恢复个人资料、站点偏好、歌单和工具箱轻量配置。
- 长列表启用延迟绘制，歌词滚动减少平滑动画，减轻页面卡顿。

## v6.4.3 - 页面贴纸轻量回归

- 各前台页面重新显示装饰贴纸，避免页面过于单调。
- 贴纸文案按首页、文章、计划、音乐、工具箱、游戏、账号和登录页分别适配。
- 贴纸动画与样式改为更轻量的低频浮动，减少滚动和切换压力。

## v6.4.2 - 黍泡泡和播放器稳定补丁

- 移除黍泡泡“仅首页”显示分支，账号页只保留显示 / 关闭，减少消失和状态不同步问题。
- Live2D 加载前直接显示静态黍泡泡，避免只出现文本框、主体不可见。
- 播放器初始化、音频元数据加载和页面卸载时不再把有效续播进度覆盖成 0。
- 非首页页面减少装饰贴纸动画层，降低切换和滚动压力。

## v6.4.1 - 侧边栏、续播和黍泡泡补丁

- 侧边栏站点名固定显示为 `FelixFu`，避免 `FelixFu's Craft` 在窄栏里被截断。
- 播放器恢复逻辑不再把已保存进度覆盖成 0，暂停和加载元数据时也会保留有效进度。
- 友链交换信息里的头像地址改为公开上传图片地址，不再使用生产环境打不开的 `/avatar.jpg`。
- 黍泡泡显示范围改由全局状态统一控制，账号页切换“全站 / 仅首页”会立刻生效。

## v6.4.0 - 阅读、计划和音乐续播体验增强

- 修复播放器离开网站后只能记住歌曲、不能稳定记住播放进度的问题。
- 音乐页新增“重洗队列”，随机播放时可以重新生成播放顺序。
- 暑期计划支持按模块控制公开范围，管理员可在后台决定哪些模块对访客可见。
- 文章目录新增当前阅读位置高亮，阅读进度计算改为按浏览器帧节流，滚动更顺。
- 工具箱补充友链交换信息，方便别人按格式添加本站。
- 账号页新增黍泡泡显示范围设置，可选全站显示或仅首页显示。

## v6.3.3 - 随机播放与收入记账

- 音乐新增随机播放模式，每次进入随机模式会生成一轮随机播放顺序。
- 随机播放模式会跟随音乐会话一起保存，下次进入网站仍会保留。
- 记账模块新增收入类型，并显示收入、支出和结余统计。

## v6.3.2 - 音乐播放位置记忆

- 记录上次播放的歌曲、歌单和播放进度，再次进入网站时从离开前的位置继续。
- 暂停、拖动进度条、播放中和关闭页面时都会保存当前播放状态。

## v6.3.1 - 友链图片上传补丁

- 工具箱 / 友链管理的图片链接旁新增上传按钮，上传成功后自动回填 `/uploads/...` 地址。
- 从微信保存下来的图片可以先作为本地文件上传，不再需要手动找公网图片链接。

## v6.3.0 - 站点设置、友链和音乐体验增强

- 工具箱和友链支持头像 / 封面图片链接，后台可统一编辑内置、自定义和友链入口。
- 备份中心新增站点 JSON 导出，文章、资料、音乐列表、歌单和工具箱配置可以一键打包。
- 音乐队列和最近播放加入歌曲封面缩略图，当前播放状态更清楚。
- 工具箱高频入口和卡片尺寸进一步收紧，页面不再显得那么撑。
- 前台图片补充懒加载和异步解码，降低滚动和首屏负担。

## v6.2.0 - 知识库、音乐和助手体验继续打磨

- Mermaid 流程图支持轻量图形预览，并保留源码切换。
- 技术笔记目录尊重后台排序，不再强制按标题字母/拼音重排。
- 后台内容库目录增加统计概览，能看到目录数、草稿数和未收纳内容。
- 黍泡泡新增低打扰模式，切页面不再强制弹话。
- 音乐歌词支持 LRC 时间戳高亮和自动滚动。
- Markdown 解析加入缓存，长文预览、目录生成和文章阅读更顺。

## v6.1.0 - 体验细化与内容系统迭代

- Markdown 预览补充 Wiki 双链、标签、Callout 和 Mermaid 代码块展示。
- 首页入口继续瘦身，只保留文章、技术笔记、音乐和工具箱四个核心入口。
- 黍泡泡增加页面感知文案，进入不同页面会说不同的话。
- 音乐页新增歌词滚动、播放队列、音量记忆和封面氛围背景。
- 后台内容库左侧目录直接展示目录下文章，更接近文件管理器视图。
- 写作台预览改为延迟渲染，输入长文时更顺滑。

## v6.0.x - 近期修复

- v6.0.6：移除黍泡泡“重置位置”入口，避免触发 Live2D 重新挂载后加载失败。
- v6.0.5：Markdown 预览支持 Setext 标题、波浪线代码块、任务列表、分割线、删除线和高亮语法。
- v6.0.4：置顶文章改为轻量胶囊开关，预计阅读时间改为按正文自动计算。
- v6.0.3：站点名称改为 `FelixFu's Craft`，公开展示名改为“副将凡”，后台支持编辑公开资料。
- v6.0.2：前台页面收紧顶部留白，黍泡泡改为右侧浮层，暑期计划默认隐藏并由后台开关控制。
- v6.0.1：工具箱管理支持编辑删除，内容库支持批量收纳笔记，迷你播放器优先显示歌曲封面。
- v6.0.0：内容管理、音乐资源、文章互动、目录跳转和后台工具箱完成一轮集中修复。

## v5.x - 内容架构成型

- v5.9：工具箱归类、排序和性能整理，友链移入工具箱分类，内容库支持拖拽排序。
- v5.8：黍泡泡改为可拖动悬浮窗，技术笔记索引和阅读侧栏整理，计划页拆成多个真实路由。
- v5.7：新增黍泡泡 Live2D 吉祥物，并修复 Cubism4 加载路径。
- v5.6：首页门户化，加入时钟、最近更新、主要入口和最近文章。
- v5.5：实现真实 URL 路由，支持 `/articles`、`/notes`、`/plan/...` 等路径。
- v5.4：版本页改为时间线，修复后台夜间模式，写作台正文预览左右对齐，侧边栏头像可修改。
- v5.3：技术笔记支持合集和目录路径，恢复早期版本清单，迷你播放器歌名横向滚动。
- v5.2：技术笔记左侧树形索引和侧边栏折叠性能优化。
- v5.1：工具箱支持管理员新增、编辑、删除、置顶和分类自定义链接。
- v5.0：内容架构重组，文章拆成“随笔 / 娱乐文章”和“技术笔记”两个入口。

## v4.x - 视觉和体验改版

- v4.3：新增工具箱；写作台删除 AI 生成历史，正文预览移到右侧，账号退出移到账号页。
- v4.2：标题字体和前台气质调整；折叠侧边栏时迷你播放器只保留播放键。
- v4.1：加入原创二次元小贴纸装饰，不影响阅读和操作。
- v4.0：前台改为更有个人气质的轻二次元视觉，降低工具台感。

## v3.x - 音乐和常驻播放

- v3.1：播放器升级为全站常驻状态，离开音乐页后侧边栏显示迷你播放器。
- v3.0：新增音乐模块，支持上传本地歌曲、自建歌单和调整播放列表。

## v2.x - 暑期计划和生活记录

- v2.6：计划表日期选择改为日历弹层，减少横向拥挤。
- v2.5：新增“我实际做了什么”表格，计算任务完成度并生成折线图和柱状图。
- v2.4：手机应用使用时间支持按日期记录，生成 7 日折线图和柱状图。
- v2.3：体重、睡眠、饮食和记账模块扩展为可编辑记录。
- v2.2：每日时间段安排支持 2026-08-04 到 2026-08-15 每天不同计划。
- v2.1：计划表接入后端数据库保存。
- v2.0：新增暑期计划板块，按时间段安排学习、运动、娱乐、阅读、饮食、睡眠和记账。

## v1.x - 博客基础功能

- v1.4：新增账号体系、评论和后台管理入口。
- v1.3：文章支持创建时间和最后修改时间。
- v1.2：新增文章详情页、阅读进度、文章目录和互动按钮。
- v1.1：文章列表、个人介绍和基础导航成型。
- v1.0：个人博客 MVP 上线。

## v0.x - 项目初始化

- v0.3：后端 FastAPI、数据库模型和 Docker Compose 初步接入。
- v0.2：React 前端骨架、侧边栏和基础页面搭建。
- v0.1：项目仓库初始化，确定个人站方向。
